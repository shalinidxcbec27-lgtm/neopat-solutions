#include<iostream>
using namespace std;
int main()
{
    int i,n,s=0,p=1;
    cin>>n;
    if(cin.fail()||n<=0)
    {
        cout<<"Invalid";
        return 0;
    }// You are using GCC
int arr[n];
for(i=0;i<n;i++)
{
    cin>>arr[i];

if(cin.fail())
{
    cout<<"Invalid";
    return 0;
}
s+=arr[i];
p*=arr[i];
}
cout<<"Sum:"<<s<<"Product:"<<p;
return 0;
}
