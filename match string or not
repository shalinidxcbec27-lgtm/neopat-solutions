#include<iostream>
#include<cstring>
using namespace std;
int main(){
    char a[100],b[100];
    cin>>a>>b;
    int s=0,i,len;
    len=strlen(a);
    for(i=0;i<len;i++)
    {
    if(a[i]!=b[i])
    {
        if((a[i]!=63)&&(b[i]!=63))
        {
            s=1;
            break;
        }
    }
    }
    ((s==0)?cout<<"Yes":cout<<"No");
}
